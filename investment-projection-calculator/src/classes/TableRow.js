//The TableRow class represents a row in a table
import Row from './Row';

class TableRow extends Row {
    constructor(period, year, age, ob, contributions) {
        super(period, year, age, ob, contributions)
    }
}

export default TableRow;